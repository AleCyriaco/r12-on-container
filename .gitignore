# Credenciais e configuracao local -- NUNCA commitar
# Local credentials and configuration -- NEVER commit
config.psd1
*.local.psd1

# Painel e atalhos GERADOS pelo New-Painel.ps1: contem todas as senhas em
# texto puro. O gerador e versionado; a saida dele, nunca.
# Dashboard and shortcuts GENERATED by New-Painel.ps1: they carry every
# password in plaintext. The generator is versioned; its output never is.
painel.html
ebs-iniciar.bat
ebs-parar.bat
ebs-status.bat

# Artefatos de execucao / runtime artifacts
logs/
vm/
pkg/
upload/
*.tar.zst
*.tar.zst.part*
manifest.txt

# Scripts .sh gerados em tempo de execucao pelo Deploy-R12.ps1.
# Contem a senha do WebLogic ja substituida -- nao devem sair da maquina.
# Runtime-generated .sh files: they carry the substituted WebLogic password.
scripts/download.sh
scripts/services.sh
scripts/extract.sh
scripts/container.sh
scripts/verify.sh
scripts/sysctl.sh
scripts/sga.sh
scripts/vm-info.sh

# Windows / editores
Thumbs.db
desktop.ini
.vscode/
NUL
